# 局域网组队（联机对战）操作说明

适用：本仓库构建出的 webroot（内网部署版）。多人对战全部走浏览器 WebRTC P2P，**不需要任何服务器中转**；唯一的辅助服务是站点同机上的 `ministun.py`（STUN，帮双方互相发现地址）。

## 先回答：组队功能测没测过？

测过，分两层：

**底层通道（真连测试，决定性）** —— 两个独立浏览器实例从站点加载游戏，剔除本机直连候选、只保留"跨机器"路径，模拟两台电脑的真实处境：

```
双方经 STUN 各拿到 srflx 地址
t+2.5s  A connected | B connected
A 收到 hello-from-B | B 收到 hello-from-A
VERDICT=PASS 仅凭非本机候选完成双向通信 → 跨机器对战可行
```

**上层房间流程（代码级核对）** —— 邀请码生成（`createRoomInvite`）、二维码/文本导入（`importPayload`）、加入响应回传、房间成员列表、地图同步，逐段核对了上游源码与 UI 组件。这部分是上游原装逻辑，没有改动，我们的补丁只动了"ICE 服务器怎么来"这一层。

**没测的**：真人从头到尾打完一整局（需要两台真机各坐一个人）。配对这步建议你找两台机器亲手走一遍 —— 底层已验证通畅，剩下的就是 UI 操作。

---

## 开始前

- 两台电脑各开 Chrome/Edge（2019 年后的版本即可），访问 `http://<服务器内网IP>:8080/`
- 服务器上确认两个服务在跑：网页（8080）+ `python3 ministun.py 0.0.0.0 3478`
- 关掉首次进入的"MOD 导入"弹窗，进主菜单

## 组队流程（4 步）

> 组队是**邀请码/二维码配对**，不是输入短码。全程一去一回两个码，之后就是普通游戏房间。

### 第 1 步：房主生成邀请

主菜单 → **局域网(LAN)** → **邀请其他玩家** → 弹出"邀请二维码"。

两种给法任选：

- **屏幕扫码**：让新玩家的浏览器开摄像头（"开启摄像头扫码"）对着房主屏幕拍
- **文本传码（推荐，两台电脑最省事）**：房主点 **复制响应内容** 旁的复制按钮，把弹窗文本框里那段 `ra2lan:...` 开头的文字，用微信/QQ/任何方式发给对方

### 第 2 步：新玩家加入

新玩家在局域网页面 → **加入房间**：

- 摄像头扫码，或"导入二维码图片"（截图发过去也行）
- 或把房主发的文本粘贴进输入框 → 点 **导入二维码内容**

### 第 3 步：响应码回传（注意：这一步容易漏）

配对是双向的。新玩家扫完/导入后，界面会生成一张 **加入响应二维码**（也是一段 `ra2lan:...` 文本）—— 必须把它传回给房主：

- 新玩家点 **复制响应内容**，发给房主
- 房主在 **接收加入响应** 区粘贴 → 点 **导入加入响应**

### 第 4 步：完成配对，开始游戏

响应导入成功后双方进入同一个房间：房主选地图、游戏模式，新玩家准备，房主点开局。地图会自动同步给新玩家（62 张对战地图已本地化）。开打。

---

## 常见问题

**卡在"等待对方"或连接失败？**
九成是 STUN 没跑。到服务器上确认：`python3 ministun.py 0.0.0.0 3478`，重启它后**刷新网页重试**。客户端会自动按站点地址找到 STUN，`config.ini` 不用改。

**摄像头打不开？**
`getUserMedia` 只在 `https://` 或 `localhost` 下可用，纯内网 IP 的 http 页面会禁用摄像头 —— 这是浏览器安全策略，不是故障。直接用**文本传码**（复制粘贴），完全等价。

**响应码导入报错？**
确认没有把"邀请码"和"响应码"搞反：房主发出去的是邀请码，收回来的应该导入到"接收加入响应"；新玩家导入邀请码，发出的是响应码。方向错了会解析失败。

**同机测试可以吗？**
可以，同一台机器开两个浏览器窗口就能走完整配对流程（不涉及跨机器，甚至不需要 STUN）。

**最多几个人？**
房间槽位受上游实现限制，界面上"当前没有空闲玩家槽位"时会提示先开空位。典型 2-4 人对战没问题。

## 给部署者的速查

```bash
# 服务器上两件事
python3 -m http.server 8080 --bind 0.0.0.0   # 在 webroot 目录里
python3 ministun.py 0.0.0.0 3478             # 任意目录，脚本同仓库

# 别把 3478/udp 映射到公网（无认证 STUN 会被当 UDP 反射放大源）
```
